class Station:
    def __init__(self, code: str, name: str, latitude: str, longitude: str, yearid: str):
        self.code = int(code)
        self.name = string(name)
        self.latitude = latitude
        self.longitude = longitude
        self.yearid = int(yearid)
